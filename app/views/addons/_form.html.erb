<%= form_with model: @addon, local: true do |form| %>
 
  <% if @addon.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@customer.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @addon.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
   <%= form.label :service %><br>
   <%= form.text_field :service %>
</p>
<p>
   <%= form.label :description %><br>
   <%= form.text_area :description %>
</p>
<p>
   <%= form.label :price %><br>
   <%= form.number_field :price %>
</p>
<p>
  <%= form.submit %>
</p>
 
<% end %>